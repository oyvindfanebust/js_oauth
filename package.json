{
  "name": "authentication",
  "version": "1.0.0",
  "description": "Library for authenticating via OAuth / Authorization Server",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Frende",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^0.4.2",
    "browserify": "^3.31.2",
    "grunt-browserify": "^1.3.1"
  }
}
